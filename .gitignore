# Ignore all files in the ignore folder
ignore/*
# But keep the folder itself tracked (via .gitkeep)
!ignore/.gitkeep

# Local White Swan AI workspace (experiments, large deps, docs with account IDs — do not commit wholesale)
White Swan AI/
